import 'package:flutter/material.dart';

class FellowshipPage extends StatelessWidget {
  const FellowshipPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Fellowhship/Scholarship page",
        ),
      ),
    );
  }
}
